import appearance from './appearance.json';
import search from './search.json';

// Master configuration object for the Browser app settings
export const browserSettingsJSON = {
  appearance,
  search
};

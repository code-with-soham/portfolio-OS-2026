// ============================================
// Portfolio OS 2026 — Configuration Constants
// ============================================

// Static file URLs
export const RESUME_URL = "/Soham_july_resume.pdf";

// Avatar is imported dynamically in components to allow Vite to bundle it,
// but if we need a static path for metadata/SEO, we define it here:
export const AVATAR_STATIC_PATH = "/profile.webp";

// Social links are now driven from profile.json (profile.social),
// but we keep this file for other OS-level constants.
